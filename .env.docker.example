# Copy to .env.docker and source before build:
#   set -a && source .env.docker && set +a && docker compose up -d --build
#
# If Docker Hub / USTC mirror fails (EOF), use a working mirror prefix:
PYTHON_BASE_IMAGE=docker.1ms.run/library/python:3.12-slim
NODE_BASE_IMAGE=docker.1ms.run/library/node:22-alpine
NGINX_BASE_IMAGE=docker.1ms.run/library/nginx:alpine
