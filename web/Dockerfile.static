# Build frontend locally first: cd web && npm install && npm run build
ARG NGINX_BASE_IMAGE=nginx:alpine
FROM ${NGINX_BASE_IMAGE}
COPY dist /usr/share/nginx/html
COPY nginx.conf /etc/nginx/conf.d/default.conf
EXPOSE 80
